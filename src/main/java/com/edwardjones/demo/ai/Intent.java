package com.edwardjones.demo.ai;

/**
 * Closed set of intents the IntentRouter can classify.
 * Do not add new values without updating IntentRouterTest.
 */
public enum Intent {
    EXPLAIN,
    WHATIF,
    COMPARE,
    PORTFOLIO_SUMMARY,
    UNSUPPORTED
}
